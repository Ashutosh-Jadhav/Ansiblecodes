- name: Ansible template Demonstration
  hosts: myhosts
  vars: 
    app_name: "MyApp"
    env: "production"
    version: "1.0.0"
    db_host: "localhost"
    db_port: 3306
    db_name: "myapp_db"
    db_user: "admin"
    db_password: "pass123"

  tasks: 
  - name: Create destination directory for configuration file
    file:
      path: /home/ashutosh/IIITB/Ansible/8th/configurations
      state: directory

  - name: Create a configuration file using template
    src: template/config.j2
    des: